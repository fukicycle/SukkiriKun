<UserControl x:Class="SukkiriKun.ShortCutItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SukkiriKun"
             mc:Ignorable="d" 
             FontSize="16"
             Height="400"
             Width="300"
             d:DesignHeight="400" d:DesignWidth="300">
    <Border Background="#363F72" CornerRadius="10" Padding="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Button Style="{StaticResource ButtonStyle}" Width="20" Height="20" Content="&#xE894;" FontFamily="Segoe MDL2 Assets" Grid.Row="0" Grid.Column="1" FontSize="14"/>
            <Image Source="{Binding Icon}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"/>
            <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Title}" TextAlignment="Center" Foreground="White"/>
        </Grid>
    </Border>
</UserControl>
